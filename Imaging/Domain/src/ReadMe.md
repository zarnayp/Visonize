# Domain namespace

**Imaging domain model and service layer**

See domain layers in Onion architecture

Domain layer is independent of the technology used to build final frame. 
It purpose is defining behaviour of ultrasound imaging bounded context.
Service layer is responsible for threading and lifecycle of domain objects. Domain objects
define behaviour.